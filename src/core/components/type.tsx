import { Canvas } from '@shopify/react-native-skia'
import { Factory } from 'native-base'
import Video from 'react-native-video'

export const NBCanvas = Factory(Canvas)
export const NBVideo = Factory(Video)
